/**
 * Supabase 环境变量（Netlify 构建时由 scripts/inject-env.js 注入）
 * 若未注入，此文件为空，将使用 supabase-config.js 中的默认配置
 */
(function(){
    // 构建时由 inject-env.js 注入 window.SUPABASE_URL 和 window.SUPABASE_ANON_KEY
})();
